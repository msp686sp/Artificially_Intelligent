"""FastAPI backend for the rental market analysis GUI.

See ``docs/gooey-plan.md`` for the build plan. This package is owned by
the eight-agent build (api-core = agent 1). Each sibling agent adds its
routes under ``api/routes/`` and registers them in
``api/routes/__init__.py`` in a labeled block.
"""

__version__ = "0.1.0"
